# Changesets

Add a changeset when a pull request changes a published package. Select the affected package, choose the required semantic-version bump, and describe the consumer-visible change.

Documentation-only and repository-maintenance changes do not require a changeset.
